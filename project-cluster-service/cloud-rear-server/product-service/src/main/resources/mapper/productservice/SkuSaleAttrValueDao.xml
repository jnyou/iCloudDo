<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.jnyou.gmall.productservice.dao.SkuSaleAttrValueDao">
    <!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="org.jnyou.gmall.productservice.entity.SkuSaleAttrValueEntity" id="skuSaleAttrValueMap">
        <result property="id" column="id"/>
        <result property="skuId" column="sku_id"/>
        <result property="attrId" column="attr_id"/>
        <result property="attrName" column="attr_name"/>
        <result property="attrValue" column="attr_value"/>
        <result property="attrSort" column="attr_sort"/>
    </resultMap>

    <select id="getSaleAttrBySpuId" resultType="org.jnyou.gmall.productservice.vo.SkuItemSaleAttrVo">
        SELECT ssav.attr_id AS attr_id,ssav.attr_name AS attr_name,GROUP_CONCAT(DISTINCT ssav.attr_value) AS attr_values
        FROM pms_sku_info info LEFT JOIN pms_sku_sale_attr_value ssav ON info.sku_id = ssav.sku_id
        WHERE info.spu_id = #{spuId,jdbcType=BIGINT} GROUP BY ssav.attr_id,ssav.attr_name
    </select>
</mapper>